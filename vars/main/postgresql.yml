---
need_restart_parameters_list_v14: [ 
  data_directory, hba_file, ident_file, external_pid_file, listen_addresses, port, max_connections, superuser_reserved_connections, unix_socket_directories, unix_socket_group,
  unix_socket_permissions, bonjour, bonjour_name, shared_buffers, huge_pages, huge_page_size, max_prepared_transactions, shared_memory_type, dynamic_shared_memory_type,
  min_dynamic_shared_memory, max_files_per_process, max_worker_processes, old_snapshot_threshold, wal_level, wal_log_hints, wal_buffers, archive_mode, recovery_target,
  recovery_target_name, recovery_target_time, recovery_target_xid, recovery_target_lsn, recovery_target_inclusive, recovery_target_timeline, recovery_target_action,
  max_wal_senders, max_replication_slots, track_commit_timestamp, hot_standby, max_logical_replication_workers, logging_collector, event_source, cluster_name,
  track_activity_query_size, autovacuum_max_workers, autovacuum_freeze_max_age, autovacuum_multixact_freeze_max_age, shared_preload_libraries, max_locks_per_transaction,
  max_pred_locks_per_transaction, data_sync_retry
  ]